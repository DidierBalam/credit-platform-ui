import { ApplicationStateType } from '../../../shared/types/store-types';

//Backup
import { applications } from '@/assets/backup/application-backup';

export const state: ApplicationStateType = {
  applications: applications,
};
